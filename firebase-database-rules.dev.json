{
  "rules": {
    "tournaments": {
      "$tournamentId": {
        ".read": true,
        ".write": "auth != null && !data.exists()",
        "matches": {
          ".write": true
        },
        "members": {
          ".write": "auth != null"
        }
      }
    }
  }
}
